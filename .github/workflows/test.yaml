name: Reusable Build and Test

on:
  workflow_dispatch: 

jobs:
  build:
    name: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: wf call
        uses: null-hyd-gha-demo/actions/test.yaml@main
